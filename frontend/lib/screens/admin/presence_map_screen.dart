import 'package:flutter/material.dart';

class PresenceMapScreen extends StatelessWidget {
  const PresenceMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Presence Map'),
      ),
      body: const Center(
        child: Text('Presence Map Screen - Coming Soon'),
      ),
    );
  }
}
